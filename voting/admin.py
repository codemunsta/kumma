from django.contrib import admin
from .models import Positions, Candidate, Result


admin.site.register(Positions)
admin.site.register(Candidate)
admin.site.register(Result)

# Register your models here.
